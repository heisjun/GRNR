import styled from 'styled-components';

const Register: React.FC = () => {
    return <StyledRegisterContainer>회원가입</StyledRegisterContainer>;
};

const StyledRegisterContainer = styled.div`
    height: 5000px;
`;

export default Register;
