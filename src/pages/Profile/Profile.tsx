import styled from 'styled-components';

const Profile: React.FC = () => {
    return <StyledProfileContainer></StyledProfileContainer>;
};

const StyledProfileContainer = styled.div`
    height: 5000px;
`;

export default Profile;
