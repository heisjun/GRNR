import styled from 'styled-components';

const Following: React.FC = () => {
    return <StyledFollowingContainer></StyledFollowingContainer>;
};

const StyledFollowingContainer = styled.div`
    height: 5000px;
`;

export default Following;
